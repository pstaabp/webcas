import type {Config} from 'jest';

const config: Config = {
	verbose: true,
  // preset: 'ts-jest',
  testEnvironment: 'node',
  // moduleNameMapper: {
  //   "/(.*)$/": "<rootDir>/src/$1",
  // },
	// roots: [
  //   "<rootDir>", "."
  // ],
  // modulePaths: [
  //   "<rootDir>/src/",
  // ],
  // moduleDirectories: [
  //   "node_modules",
	// 	"src"
  // ],
};

export default config;
